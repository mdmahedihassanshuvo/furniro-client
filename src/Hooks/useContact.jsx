import { useQuery } from '@tanstack/react-query';
import React from 'react';

const useContact = () => {
    const {} = useQuery({
        
    })
    return (
        <div>
            
        </div>
    );
};

export default useContact;